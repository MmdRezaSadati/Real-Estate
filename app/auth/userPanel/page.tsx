import Box from "@mui/material/Box";
import React from "react";
import { PanelProvider } from "./resources";
import Grid from "@mui/material/Grid";

const UserPanel = () => {
  return (
    <PanelProvider>;
    </PanelProvider>
  );
};

export default UserPanel;
